spring.application.name=seasonservice

server.port=8084

# Database connection properties
spring.datasource.url=jdbc:mysql://localhost:3306/seasonservice
spring.datasource.username=root
spring.datasource.password=8618609838
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Allow bean definition overriding if necessary
spring.main.allow-bean-definition-overriding=true

# Actuator properties
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
management.info.env.enabled=true
management.endpoint.shutdown.enabled=true
server.shutdown=graceful

info.app.name = Netflix Clone
info.app.description = A Netflix clone using Spring Boot and React.js.
info.app.version = 1.0.0

#Eureka
eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true